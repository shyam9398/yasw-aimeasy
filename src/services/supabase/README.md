# Supabase Integration

Add these values to `.env.local` when you are ready to connect the backend:

```text
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
```

Then import `supabase` from `src/services/supabase/client.js`.
